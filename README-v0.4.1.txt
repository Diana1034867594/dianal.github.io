XINYU PORTFOLIO v0.4.1 — COMPLETE CACHE-FIXED PACKAGE

This package includes the complete site, not only three patch files.

Visible changes:
- Projects uses visual case-study cards instead of accordions.
- Each card opens its matching project page.
- Project type, introduction, and tool tags are displayed.
- Category jump links appear near the top of Projects.
- Projects dropdown remains in the navigation.
- Posts is removed from the navigation on every page.
- Site title is Xinyu Liao on every page.
- Footer uses the new professional introduction on every page.
- posts.html now redirects to projects.html.

Important cache fix:
All pages now load:
- css/style-v0.4.1.css
- js/components-v0.4.1.js

Because the filenames are new, the browser cannot reuse the old cached header/footer.

INSTALLATION
1. In GitHub Desktop: Repository > Show in Explorer.
2. Open this extracted package.
3. Copy EVERYTHING INSIDE it into the repository root.
4. Choose Replace files in the destination.
5. Confirm GitHub Desktop shows many modified HTML files plus new versioned CSS/JS files.
6. Commit: Apply complete v0.4.1 redesign
7. Push origin.
